{
  "$schema": "https://turbo.build",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true,
      "passThroughEnv": ["GOCACHE", "GOPATH", "LOCALAPPDATA", "USERPROFILE", "PATH"]
    },
    "build": {
      "outputs": ["dist/**", ".svelte-kit/**"]
    }
  }
}